let login = prompt("ur name, slave");

if (login == null) {
    alert("fucking slave");
}
else{

if (login == 'master') {
    let password = prompt("password");
    if (password == null) { alert("cancelled"); }
    if (password == "oralcumshot") { alert("welcome to the dungeon, master"); }
    else { alert("fucking slave"); }
}
else { alert("slave!"); }}
